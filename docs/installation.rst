============
Installation
============

At the command line::

    $ easy_install ae_okta_auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ae_okta_auth
    $ pip install ae_okta_auth
